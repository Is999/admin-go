# 组件注册清单

本文档记录 admin 默认组件、路由模块、任务插件和运行时扩展注册入口。新增默认能力时优先修改对应规格，避免注册入口散落在 main、bootstrap、handler 或业务逻辑中。

清单项必须区分“生产已启用”和“基础能力/未激活”：前者能从默认启动规格、配置或真实业务调用方到达最终副作用；后者提供稳定扩展入口、多个已有实现或依赖隔离，但默认命令尚未调用扩展方法。`manifest.Default()` 用于汇总和校验清单，不负责启动能力，清单存在本身不能证明生产已启用，也不能据此删除未激活的基础能力。

## 统一入口

| 注册类型 | 统一入口 | 说明 |
| --- | --- | --- |
| 启动组件 | `internal/bootstrap/components/builtin/specs.go:DefaultSpecs` | 统一声明运行告警、安全缓存补偿、通用收集器、任务运行时、HTTP 服务组件顺序，并派生真实组件和清单 |
| 启动组件执行器 | `internal/bootstrap/components:NewRegistry` | 通用执行器负责顺序注册和名称唯一校验 |
| HTTP 路由 | `internal/handler/routes.go:builtinRouteModuleSpecs` + 各模块 `RouteSpecs` | 统一声明内置路由模块顺序和路由规格来源，真实注册、`RouteContract` 和注册清单从规格派生 |
| 任务插件 | `internal/bootstrap/components/builtin/task.go:DefaultTaskPluginSpecs` + `internal/jobs/plugins.go:PluginSpecs` | 统一声明核心、业务和维护任务插件顺序，任务组件和注册清单从规格派生 |
| 运行时扩展 | 各包 `RuntimeRegistrySpecs` | `pkg/storage`、`internal/logic/file`、`internal/infra/collectorx` 分别声明自身注册入口，`bootstrap` 统一汇总清单 |

## 启动组件

| 名称 | 注册文件 | 注册方法 | 生命周期 | 说明 |
| --- | --- | --- | --- | --- |
| `runtime_alert` | `internal/bootstrap/components/builtin/alert.go` | `newRuntimeAlert` | 启动装配 | 注册独立 Lark 运行告警入口，不依赖任务系统开关 |
| `security_cache_sync` | `internal/bootstrap/components/builtin/security_cache_sync.go` | `newSecurityCacheSync` | `State.AddLifecycleHooks` | 启动前处理历史安全缓存失效任务，运行期持续补偿 |
| `collector` | `internal/bootstrap/components/builtin/collector.go` | `newCollector` | `State.AddLifecycleHooks` | 创建通用收集器，注册 `admin_log.audit` 批量入库 Processor，Worker 模式启动 Kafka 消费和失败账本重试协程 |
| `cdc_consumer` | `internal/bootstrap/components/builtin/cdc.go` | `newCDCConsumer` | `State.AddLifecycleHooks` | 创建 CDC 消费器，并注册 Debezium 表处理器 |
| `task_runtime` | `internal/bootstrap/components/builtin/task.go` | `newTaskRuntime` | `State.AddLifecycleHooks` | 创建 `taskqueue.Manager`，并通过任务插件规格注册任务插件 |
| `http_server` | `internal/bootstrap/components/builtin/http.go` | `newHTTPServer` | HTTP Server 自身生命周期 | 创建公网与内网两个 REST Server，并按路由访问类型分区注册 |

## HTTP 路由模块

| 名称 | 注册文件 | 路由规格入口 | 说明 |
| --- | --- | --- | --- |
| `health` | `internal/handler/health/routes.go` | `health.RouteSpecs` | 健康检查路由 |
| `auth` | `internal/handler/auth/routes.go` | `auth.RouteSpecs` | 登录、登出、有效会话主动续签 |
| `admin` | `internal/handler/admin/routes.go` | `admin.RouteSpecs` | 管理员管理接口 |
| `user` | `internal/handler/user/routes.go` | `user.RouteSpecs` | 前台用户与 API 运行态管理接口 |
| `profile` | `internal/handler/profile/routes.go` | `profile.RouteSpecs` | 个人中心与账号安全接口 |
| `rbac` | `internal/handler/rbac/routes.go` | `rbac.RouteSpecs` | 角色权限接口 |
| `config` | `internal/handler/config/routes.go` | `config.RouteSpecs` | 系统配置接口 |
| `cache` | `internal/handler/cachemanage/routes.go` | `cachemanage.RouteSpecs` | 缓存管理接口 |
| `admin_log` | `internal/handler/admin/routes.go` | `admin.LogRouteSpecs` | 管理员审计日志接口 |
| `secret_key` | `internal/handler/secretkey/routes.go` | `secretkey.RouteSpecs` | 秘钥管理接口 |
| `security_debug` | `internal/handler/securitydebug/routes.go` | `securitydebug.RouteSpecs` | 安全调试接口 |
| `message` | `internal/handler/message/routes.go` | `message.RouteSpecs` | 消息中心接口 |
| `transfer` | `internal/handler/transfer/routes.go` | `transfer.RouteSpecs` | 文件传输、断点续传上传 |
| `task` | `internal/handler/task/routes.go` | `task.RouteSpecs` | 任务总控、队列、任务列表与监控 |
| `runtime_config` | `internal/handler/runtimeconfig/routes.go` | `runtimeconfig.RouteSpecs` | 运行配置草稿、发布与回滚接口 |
| `collector` | `internal/handler/collector/routes.go` | `collector.RouteSpecs` | 通用收集器管理接口 |
| `user_tag` | `internal/handler/usertag/routes.go` | `usertag.RouteSpecs` | 用户标签业务接口 |
| `internal_user_tag` | `internal/handler/usertag/routes.go` | `usertag.InternalRouteSpecs` | 内网用户标签接口 |
| `internal_auth` | `internal/handler/auth/routes.go` | `auth.InternalRouteSpecs` | 内网管理员自举接口 |
| `docs` | `internal/handler/docs/routes.go` | `docs.RouteSpecs` | API 文档服务 |

## 任务插件

| 名称                        | 注册文件                                                                                                 | 构造方法                                                                  | 主要注册内容                                                     |
|---------------------------|------------------------------------------------------------------------------------------------------|-----------------------------------------------------------------------|------------------------------------------------------------|
| `core`                    | `internal/task/runtime/core_plugin.go`                                                              | `taskruntime.NewCorePlugin`                                           | 工作流触发、空节点、缓存刷新 handler、缓存刷新聚合器、缓存刷新工作流                     |
| `job_task_metadata`       | `internal/jobs/plugins.go`                                                                          | `jobs.TaskMetadataPlugin`                                             | 业务任务类型和工作流展示元数据                                        |
| `task_history`            | `internal/jobs/taskhistory/plugin.go`                                                               | `taskruntime.NewPluginFunc` / `taskhistory.Setup`                     | 终态历史有界异步落库、短期查询和工作流 DB 兜底                         |
| `archive`                 | `internal/jobs/archive/task/plugin.go`                                                              | `taskruntime.NewPluginFunc` / `archivetask.Setup`                     | 通用归档 handler 和归档工作流                                        |
| `excel_export`            | `internal/jobs/export/plugin.go`                                                                    | `taskruntime.NewPluginFunc` / `exportjob.Setup`                       | 管理员和前台用户列表异步导出 handler                                      |
| `file_upload_cleanup`     | `internal/jobs/file_cleanup.go`                                                                     | `jobs.FileUploadCleanupPlugin`                                       | 上传会话失效后的精确对象清理 handler                                      |
| `task_report`             | `internal/jobs/taskreport/task/plugin.go`                                                           | `taskruntime.NewPluginFunc` / `taskreporttask.Setup`                  | 周期任务和工作流运行日报 handler 与工作流                             |
| `user_tag`                | `internal/jobs/usertag/task/plugin.go`                                                              | `taskruntime.NewPluginFunc` / `usertagtask.Setup`                     | 用户标签节点 handler、周期维护任务和 full/delta/targeted/recalculate 工作流 |

## 运行时扩展注册表

| 名称 | 注册文件 | 注册方法 | 当前状态 | 职责与启用依据 |
| --- | --- | --- | --- | --- |
| `object_storage` | `pkg/storage/registry_specs.go` / `pkg/storage/storage.go` | `storage.RuntimeRegistrySpecs` / `RegisterObjectStorage` | 基础能力；内置实现已启用，外部注册入口未激活 | `local`、`s3` 两个工厂由 `NewObjectStorage` 按 `file_storage.type` 选择；`RegisterObjectStorage` 保留为自定义后端扩展契约，当前默认命令没有额外注册调用方 |
| `file_upload_policy` | `internal/logic/file/registry_specs.go` / `internal/logic/file/file_transfer_policy.go` | `file.RuntimeRegistrySpecs` / `RegisterFileUploadPolicy` | 基础能力；内置策略已启用，外部注册入口未激活 | 头像和系统配置导入策略由上传链路读取；`RegisterFileUploadPolicy` 允许业务模块增加受校验策略，当前默认命令没有额外注册调用方 |
| `collector_processor` | `internal/infra/collectorx/registry_specs.go` / `internal/infra/collectorx/manager.go` | `collectorx.RuntimeRegistrySpecs` / `RegisterProcessor` / `RegisterProcessorFunc` | 生产已启用 | Collector 启动组件实际注册 `admin_log.audit` 和 `auth.security` Processor，业务通过相同 bizType 投递 Kafka 事件 |

## 单库数据库路由

| 场景 | 统一入口 | 说明 |
| --- | --- | --- |
| 默认物理库 | `mysql` 配置 | 当前唯一必填 MySQL 入口 |
| 主库读连接 | `internal/svc/dbrouter.go:ServiceContext.ReadDB` | 空值或 `main` 指向主库只读连接 |
| 主库写连接 | `internal/svc/dbrouter.go:ServiceContext.WriteDB` | 空值或 `main` 指向主库写连接 |
| 拆库扩展 | `site_mysql.<name>` | 可选命名扩展库，默认不配置 |
| 归档控制库 | `internal/jobs/archive/service.go:WithControlDatabase` | 默认使用主库 |
| 归档源库 | `internal/jobs/archive/service.go:jobSourceWriteDB` | 由 `archive_jobs[].database` 指定，默认 `main` |

更完整的约束、接入步骤和禁止项，见 `库表路由规范.md`。

默认所有表都使用同一个 `mysql` 物理库；需要拆库时通过 `site_mysql.<name>` 注册命名扩展库。

当前默认归属：

| 表                                                                           | 默认数据库    | 说明                                                                                           |
|-----------------------------------------------------------------------------|----------|----------------------------------------------------------------------------------------------|
| `archive_segment` / `archive_watermark`                                     | `main`   | 归档控制表                                                                                        |
| `collector_failed_event`                                                          | `main`   | Collector 失败事件、死信和人工重试表                                                                  |
| `security_cache_sync_task`                                                        | `main`   | RBAC、管理员会话和 MFA 精确失效补偿任务                                                               |
| `sys_config`                                                                | `main`   | 系统配置表                                                                                        |
| `secret_key` / `secret_key_version`                                         | `main`   | 接口安全秘钥配置表                                                                                    |
| `admin_message` / `admin_message_receiver` / `admin_log`                    | `main`   | 管理员消息和审计日志表                                                                                  |
| `user_tag_*` / `user_tag_runtime_uid` / `user_tag_runtime_checkpoint` / `user_tag_event_outbox` | `main` | 用户标签骨架运行期表 |

## 外部运行期配置

| 场景     | 配置入口                   | 说明                                                                                                            |
|--------|------------------------|---------------------------------------------------------------------------------------------------------------|
| 运行期小配置/兼容种子 | `config_files.runtime` | `workflows` 仍由文件管理；`task_periodic`、`archive_jobs` 在 DB 模式下仅作为手动导入或草稿为空时的兼容种子 |

主配置只保留 `task` 基础参数和 `scheduler`、`archive` 基础参数；`task.periodic`、`archive.jobs` 必须通过运行配置草稿发布，顶层 `user_tag` 迁移到运行期配置文件的 `workflows.user_tag`。

首次导入种子文件格式：

```yaml
task_periodic:
  - name: "user-tag-delta-daily"
    cron: "0 3 * * *"
    workflow: "user_tag.delta.refresh"
    targets: ["dry_run=1"]
  - name: "cache-refresh-every-10s"
    cron: "*/10 * * * * *"
    workflow: "cache.refresh"
  - name: "user-tag-runtime-cleanup"
    every_seconds: 21600
    workflow: "user_tag.runtime.cleanup"
  - name: "user-tag-event-outbox-retry-scan"
    every_seconds: 600
    workflow: "user_tag.event_outbox.retry_scan"

archive_jobs:
  - name: "admin_log"
    enabled: true
    database: "main"
    table_name: "admin_log"
    archive_condition: ""
    delete_condition: ""
    archive_delay_days: 2
    archive_window_seconds: 3600
    delete_delay_days: 30
    delete_window_seconds: 3600

workflows:
  user_tag:
    enabled: true
    default_shard_total: 1

```

文件模式热加载会把主配置和 `config_files.runtime` 指向的外部文件一起计算指纹；数据库模式下 `task_periodic/archive_jobs` 以 active release 为准。

本地运行时从项目根目录执行 `go run ./cmd/admin -f etc/config.yaml -mode 7`，`config_files.runtime: "config.d/runtime.yaml"` 会解析到 `etc/config.d/runtime.yaml`。

任务插件启停、handler 注册和 workflow 定义注册属于启动期能力。热加载可以刷新批次、分片、限速等运行参数，但不能把启动时未注册的插件在线补注册；遇到 `restartRequired=true` 且 `restartReason` 包含插件启停项时，需要重启对应进程。

## 扩展规则

1. 新增默认启动组件：在 `builtin.DefaultSpecs` 增加规格，由 `builtin.DefaultComponents` 和 `manifest.Default()` 自动派生。
2. 新增默认路由模块：先在对应模块维护 `RouteSpecs`，再在 `internal/handler/routes.go:builtinRouteModuleSpecs` 增加规格来源；真实注册、`DefaultRouteContracts` 和注册清单会从规格派生。
3. 新增默认任务插件：先在 `internal/jobs/<业务>/task` 定义 `Setup(...)`，再加入 `internal/jobs/plugins.go:PluginSpecs`；启动链和注册清单会从规格派生。
4. 新增 Collector bizType：通过 `Collector.RegisterProcessor` 或 `Collector.RegisterProcessorFunc` 注册批量消费逻辑。
5. 新增文件上传业务类型：通过 `file.RegisterFileUploadPolicy` 注册，并保证扩展名、大小、可见性都有明确策略。
6. 新增默认运行时扩展入口：先说明它属于生产已启用还是基础能力/未激活；在能力归属包维护 `RuntimeRegistrySpecs`，`bootstrap` 从规格派生注册清单。只有真实调用方、启用配置和最终副作用接通后才能标记生产已启用；基础能力必须有稳定职责、接入约束和聚焦测试，不能只留清单占位。
7. 调整表归属库：优先判断是否仍可留在默认 `mysql`；确需拆库时补 `site_mysql.<name>` 并通过 `ReadDB` / `WriteDB` 访问对应名称。
8. 所有默认注册名称必须唯一；启动装配和 `go test ./internal/bootstrap ./internal/handler` 会校验真实注册集合的名称冲突。
