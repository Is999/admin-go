# 秘钥管理接口

## 接口总览

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/api/secret-keys` | 查询秘钥列表 |
| `GET` | `/api/secret-keys/:id` | 查询秘钥详情 |
| `POST` | `/api/secret-keys` | 新增秘钥 |
| `PATCH` | `/api/secret-keys/:id` | 编辑秘钥 |
| `PATCH` | `/api/secret-keys/status/:id` | 启用或停用秘钥 |
| `POST` | `/api/secret-keys/cache/refresh/:uuid` | 刷新秘钥缓存 |
| `POST` | `/api/secret-keys/validate` | 预检秘钥路径与材料可用性 |
| `POST` | `/api/secret-keys/self-check/:uuid` | 执行运行态自检并刷新缓存 |

## 查询秘钥列表

```http
GET /api/secret-keys?uuid=admin-1&title=管理&page=1&pageSize=10
```

查询参数：

| 字段 | 类型 | 是否必须 | 说明 |
| --- | --- | --- | --- |
| uuid | string | 否 | AppID / API KEY 唯一标识筛选 |
| title | string | 否 | 秘钥标题筛选 |
| status | int | 否 | 应用状态：`1` 启用，`0` 禁用 |
| signStatus | int | 否 | 签名验签状态：`1` 启用，`0` 停用 |
| cryptoStatus | int | 否 | 加密解密状态：`1` 启用，`0` 停用 |
| stableVersion | string | 否 | 稳定版本筛选 |
| page | int | 否 | 页码，默认 `1` |
| pageSize | int | 否 | 每页数量，默认 `10`，最大 `100` |
| orderBy | string | 否 | 排序字段，未传默认按 ID 排序 |
| order | string | 否 | `asc` 或 `desc` |

响应 `data` 为分页结构：`list` 是 `SecretKeyItem[]`，`total` 是命中总数。列表接口会脱敏路径类敏感字段，`secretMasked=true` 表示当前响应展示值已脱敏。

## 查询秘钥详情

```http
GET /api/secret-keys/1?keyVersion=v1&twoStepKey=ticket_xxx&twoStepValue=proof_xxx
```

详情接口用于编辑场景读取完整配置，必须完成秘钥管理 MFA 二次确认。

| 字段 | 类型 | 是否必须 | 说明 |
| --- | --- | --- | --- |
| id | int | 是 | 路径参数，秘钥 ID |
| keyVersion | string | 否 | 指定查看的版本号；未传默认稳定版本 |
| twoStepKey | string | 是 | MFA 二次校验票据 Key，场景 `MFAScenarioSecretKeyManage = 11` |
| twoStepValue | string | 是 | MFA 二次校验票据值 |

响应 `data` 为 `SecretKeyItem`，并带 `versionList` 返回当前 AppID 下的全部版本。

## 秘钥保存请求

```json
{
  "uuid": "1",
  "title": "管理后台前端应用",
  "keyVersion": "v1",
  "stableVersion": "v1",
  "grayVersion": "v2",
  "grayPercent": 10,
  "aesKeyRef": "/etc/admin/keys/1/v1/aes_key",
  "aesIvRef": "/etc/admin/keys/1/v1/aes_iv",
  "rsaPublicKeyUserRef": "/etc/admin/keys/1/v1/user_public.pem",
  "rsaPrivateKeyServerRef": "/etc/admin/keys/1/v1/server_private.pem",
  "status": 0,
  "signStatus": 1,
  "cryptoStatus": 1,
  "versionStatus": 1,
  "remark": "部署校验通过后启用"
}
```

> **注意**：当前项目的秘钥管理已经升级为“主配置 + 版本配置”模式。`secret_key` 主表管理稳定版本、灰度版本、灰度比例、应用状态，以及 `signStatus` / `cryptoStatus` 两个运行时安全开关；具体 AES/RSA 材料全部落在 `secret_key_version` 表。系统只允许保存文件绝对路径，不再接受 AES 明文或 RSA PEM 文本。新增、编辑、切换状态、刷新缓存与运行态自检均属于敏感操作，使用 `MFAScenarioSecretKeyManage = 11`；后台配置需要 MFA 时必须提交 `twoStepKey`、`twoStepValue`。
>
> - `signStatus=1` 时，当前 AppID 才会执行请求验签和响应回签
> - `cryptoStatus=1` 时，当前 AppID 才会执行请求解密和响应加密
> - `signStatus` 与 `cryptoStatus` 相互独立，允许只启用其中一条链路
> - 两个链路都关闭时，不再强制要求对应材料参与启用校验
> - `rsaPublicKeyServerRef` 可显式配置服务端 RSA 公钥路径；为空时由服务端私钥派生公钥
> - 写操作已提交但材料或路由缓存刷新失败时返回 `data.syncPending=true`；恢复 Redis 后使用现有缓存刷新入口，不要重复提交写操作
> - 每个 AppID 最多保存 `100` 个材料版本；已有版本达到上限后仍可编辑，但提交新的 `keyVersion` 会返回参数错误。系统不会自动删除旧版本，清理前必须确认稳定版、灰度版和客户端显式版本均不再引用。

## 预检接口

用于在保存前检查路径格式、文件是否存在、AES 长度、RSA PEM 格式以及服务端密钥是否可用。

```json
POST /api/secret-keys/validate
{
  "uuid": "1",
  "title": "管理后台前端应用",
  "keyVersion": "v2",
  "stableVersion": "v1",
  "grayVersion": "v2",
  "grayPercent": 20,
  "aesKeyRef": "/etc/admin/keys/1/v2/aes_key",
  "aesIvRef": "/etc/admin/keys/1/v2/aes_iv",
  "rsaPublicKeyUserRef": "/etc/admin/keys/1/v2/user_public.pem",
  "rsaPrivateKeyServerRef": "/etc/admin/keys/1/v2/server_private.pem",
  "status": 1,
  "signStatus": 1,
  "cryptoStatus": 1,
  "versionStatus": 1,
  "twoStepKey": "xxx",
  "twoStepValue": "xxx"
}
```

返回结果会包含 `allPassed`、`canSave`、`canEnable` 与 `items[]` 分项结果；前端只展示校验结果，不返回文件真实内容和内部错误链。

## 运行态自检接口

用于对已落库 `uuid` 执行缓存刷新、AES 加解密、RSA 签名验签、RSA 加解密链路检查。

```json
POST /api/secret-keys/self-check/YWRtaW4wMDAx
{
  "keyVersion": "v1",
  "twoStepKey": "xxx",
  "twoStepValue": "xxx"
}
```

返回结果会额外包含：

- `runtimeChecked=true`
- `cacheRefreshed=true/false`
- `durationMs`
- `items[]` 中的运行态分项结果，如 `AES加解密自检`、`RSA验签自检`

## 运行时缓存与灰度

- 路由缓存：`secret_key_route:{uuid}`
- 版本材料缓存：`secret_key_aes:{uuid}:{keyVersion}`、`secret_key_rsa:{uuid}:{keyVersion}`
- 版本材料索引：`secret_key_version:index:{uuid}`，成员为当前 AppID 下已写入的 AES/RSA 版本材料真实 key；刷新和删除按索引精确清理，不使用前缀 `SCAN`
- 未显式指定版本时，运行时先命中 `stableVersion`
- 显式传入的 `X-Key-Version` 最长 64 位，不能包含空白字符或通配符；不存在的随机版本不会写入空值缓存，避免被恶意制造高基数 Redis Key
- 当 `grayVersion` 非空且 `grayPercent > 0` 时，按 `X-Gray-Key` 进行稳定分桶灰度
- 服务端会把最终命中的版本回写到响应头 `X-Key-Version`

## RSA 工具说明

秘钥管理页的 RSA 工具已调整为前端本地生成与下载，不再提供服务端 `generate/download` 接口。运维需要的用户公钥和服务端私钥应在浏览器本地生成后离线分发到挂载目录，再由秘钥配置引用对应文件绝对路径。

## 统一响应字段说明

| 字段 | 类型 | 是否必须 | 默认值 | 字段解释 |
|------|------|----------|--------|----------|
| status | bool | 是 | false | 状态: true 成功, false 失败 |
| code | int | 是 | 1 | 业务状态码 |
| message | string | 否 |  | 消息 |
| data | object/array/null | 否 |  | 秘钥列表、秘钥详情、预检结果或自检结果 |
| data.syncPending | bool | 否 | false | 写操作已提交但秘钥缓存仍待刷新 |

### 秘钥 data 内部字段

| 字段 | 类型 | 是否必须 | 默认值 | 字段解释 |
|------|------|----------|--------|----------|
| id | int | 是 |  | 秘钥 ID |
| uuid | string | 是 |  | 应用标识 |
| title | string | 是 |  | 应用名称 |
| stableVersion | string | 否 |  | 稳定版本 |
| grayVersion | string | 否 |  | 灰度版本 |
| grayPercent | int | 否 | 0 | 灰度比例 |
| signStatus | int | 是 | 0 | 签名状态 |
| cryptoStatus | int | 是 | 0 | 加密状态 |
| status | int | 是 | 0 | 应用状态 |
